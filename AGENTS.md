# C'mon Yo! 작업 지침

- 사용자와 한국어 존댓말로 소통합니다. 현재 사용자 지시가 문서 속 실행 예시보다 우선합니다.
- 공개 문서·이슈·PR·커밋에는 서비스의 사용자 문제, 구현 선택과 근거, 검증 결과, 후속 작업을 기록합니다.
- 새 커밋은 영어 type·scope와 한국어 제목·본문으로 작성합니다. 코드 식별자·제품명·명령은 원래 표기를 유지합니다.
- `CONTRIBUTING.md`의 브랜치·커밋·PR 규칙을 따릅니다. 저자는 heznpc 단독이며 공개 메타데이터에 PII를 넣지 않습니다.
- 변경은 적절한 검증·커밋·푸시까지 진행합니다. 새 공개 표면의 최초 발행과 파괴적인 Git 조작은 별도 승인이 필요합니다.
- PR1 범위는 `docs/pr1-acceptance.md`입니다. PR1 작업에는 DB/auth/GPS/HealthKit/댓글 쓰기를 섞지 않으며, 이후에는 확정된 사용자 시나리오와 해당 기능 범위에 따라 구현합니다. 범용 framework를 선행 구축하지 않습니다.
- 승인된 구현 범위는 단계마다 재승인을 기다리지 않고 구현·테스트·실행 QA·수정을 이어갑니다. PR은 검토 단위이며 다음 작업의 자동 중단 지점이 아닙니다. 외부 키가 없으면 로컬 DB·시험 공급자로 독립 구현을 계속하고, 유효한 설정을 넣은 공식 실연결은 별도로 검증합니다.
- API와 SSR loader는 같은 service를 직접 호출합니다. server-only 의존성을 browser bundle에 넣지 않습니다.
- React와 SwiftUI는 별도 제품 화면입니다. WebView 경계는 현재 읽기 전용 통합 시험을 넘어 임의로 확정하지 않습니다.
- 테스트 통과, 실제 실행, 성능 검증을 서로 구분합니다. 필수 검증이 남으면 미검증으로 기록하고 PR을 draft로 유지합니다.
- 각 PR에서 발견한 결함을 수정한 뒤 실패했던 흐름을 다시 검증합니다.
- 원격 푸시 전에 설명적인 중립 브랜치 이름을 사용합니다. 다른 세션의 워크트리·브랜치·Simulator를 삭제하지 않습니다.

## 모든 기능 단계의 UI·스타일 작업

- 최종 브랜드 시안은 선행조건이 아닙니다. 브랜드 미정의 기본 UI로 기능을 구현하며 기존 시안이나 임의 브랜드를 승인한 것으로 해석하지 않습니다.
- 정보 순서·대비·작은 화면·긴 제목·글자 확대·키보드·포커스·터치, 로딩·404·연결 실패·재시도를 기능 단계에서 검증합니다.
- 실제 사용하는 컴포넌트와 스타일 변경 지점만 정리합니다. 미래 기능용 범용 컴포넌트나 디자인 시스템을 만들지 않습니다.
- 승인된 브랜드는 후속 PR에서 묶어 적용할 수 있으며 기능 PR마다 승인을 기다리지 않습니다. QA 범위는 실제 변경 영향으로 정하고 기능 테스트를 임시 CSS 클래스나 DOM 순서에 결합하지 않습니다.
- 기능 구현에는 기본 배치·가독성·반응형·포커스·터치 영역을 제공하고, 색상·서체·장식 등의 최종 브랜드 스타일은 결정 후 Web CSS와 Native 스타일에 묶어 적용합니다. 기능·API 상태 분기를 브랜드 값에 결합하지 않습니다. 공통 스타일 값은 실제 반복 사용처가 있을 때 모으며 일괄 적용이 값 치환만으로 끝난다고 가정하지 않습니다.
