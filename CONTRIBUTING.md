# 작업 규칙

## 브랜치와 커밋

- 기본 브랜치는 `main`입니다. 기능 작업은 이슈에 연결된 별도 브랜치에서 진행합니다.
- 브랜치: `feat/<topic>`, `fix/<topic>`, `chore/<topic>`, `docs/<topic>`, `refactor/<topic>`, `test/<topic>`.
- 원격 브랜치에 도구 이름 접두어를 사용하지 않습니다. 워크트리와 브랜치를 임의로 삭제하지 않습니다.
- 커밋과 PR 제목: `type(scope): description`. scope는 선택 사항이며 제목은 구체적인 변경을 설명합니다.
- type: `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `build`, `ci`, `chore`, `revert`.
- 예: `feat(meetups): connect native detail to the HTTP API`.
- 저자는 `heznpc`, 이메일은 `222764483+heznpc@users.noreply.github.com`만 사용합니다. 개인 이메일이나 공동 저자 trailer를 넣지 않습니다.
- merge 방식은 squash를 기본으로 합니다. force push, hard reset, 미병합 브랜치 삭제는 사전 승인 없이 하지 않습니다.

## 이슈 → PR → 검증

1. 이슈에 사용자 행동, 범위, 제외 범위, 수용조건을 적습니다.
2. 수용조건에 필요한 코드와 자동 테스트를 같은 PR에서 구현합니다.
3. 실제 실행 환경에서 해당 사용자 흐름을 QA합니다. UI는 화면·상호작용·오류 상태를 확인합니다.
4. 발견한 결함을 수정하고, 실패했던 흐름과 영향받은 검사를 다시 실행합니다.
5. PR 템플릿에 실행 명령·환경·관측 결과·수정한 결함·미검증 항목을 기록합니다.
6. 검증 결과가 반영된 커밋을 푸시하고 PR을 갱신합니다. 문서 검토나 단위 테스트만으로 실행 완료를 주장하지 않습니다.

각 PR의 변경 범위에 맞는 검증을 수행합니다. 문서·템플릿 PR에는 문서 링크·형식·운영 규칙 확인을 적용하며, UI 없는 변경에 화면 QA를 요구하지 않습니다. 적용되지 않는 항목은 이유와 함께 N/A로 기록합니다.

## 완료 판정

- 기능 PR은 자동 검사 통과와 해당 실행 흐름의 QA가 모두 필요합니다.
- Web은 개발 실행과 production build 실행을 구분합니다. SSR 본문, hydration, 오류, 필요한 반응형 화면을 확인합니다.
- iOS는 빌드·단위 테스트·Simulator 실행·실기기 검증을 구분합니다. 실제 사용한 OS·기기와 결과를 남깁니다.
- 성능은 환경·데이터·시나리오·cold/warm·표본·집계 기준을 기록합니다. 측정하지 않은 개선율과 운영 수용량을 주장하지 않습니다.
- 필수 실행 검증이 남으면 PR을 draft로 유지합니다. 후속 범위는 별도 이슈로 분리하되 현재 수용조건을 몰래 축소하지 않습니다.
- QA 증거에는 credential, 개인 이메일, raw 위치, 건강 원본, 로컬 사용자 경로를 포함하지 않습니다.
- 새로운 공개 서비스나 공개 저장소의 최초 발행은 별도 승인을 받습니다.

## 현재 단계

PR1은 fixture 기반 공개 모임의 API·SSR·SwiftUI·읽기 전용 WebView 연결입니다. 이후 PR도 동일한 구현 → 테스트 → 실행 QA → 디버깅 → 재검증 흐름을 따릅니다. 자세한 완료 조건은 [PR1 수용조건](docs/pr1-acceptance.md)을 따릅니다.
